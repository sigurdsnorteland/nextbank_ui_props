export interface ButtonGroupProps_v1 {
    /** Applies the thin modifier to remove margins */
    thin?: boolean;
    /** Applies the inline modifier to make all child buttons inline */
    inline?: boolean;
    /** Label for the group of buttons for users with screen reader*/
    ariaLabel?: string;
}