export interface CardGroupTitleProps_v1 {
    /** No padding on the element */
    noPadding?: boolean;
    /** Visible border underneath title*/
    noSeparator?: boolean;
    children?: React.ReactNode;
};