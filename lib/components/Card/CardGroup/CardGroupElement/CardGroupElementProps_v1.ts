export interface CardGroupElementProps_v1 {
    /** No padding on the element */
    noPadding?: boolean;
    /** Visible border between the elements*/
    noSeparator?: boolean;
    children?: React.ReactNode
};